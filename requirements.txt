# Pinned to the versions this repo was developed and tested against.
# transformers is pinned hard: src/regraph/model.py contains a manual layer loop
# written against the exact signatures of transformers 4.57.3 modeling_llama.py.
torch==2.10.0
transformers==4.57.3
torch-geometric==2.6.1
sentence-transformers==5.5.1
datasets==4.0.0
numpy>=2.0
pandas>=2.0
scikit-learn>=1.3
PyYAML>=6.0
tqdm
pytest
